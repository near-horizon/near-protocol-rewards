# Database Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=near_rewards_test
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# API Configuration
GITHUB_TOKEN=
NEAR_API_URL=https://api.nearblocks.io/v1
NEAR_NETWORK_ID=mainnet

# Server Configuration
PORT=3000
NODE_ENV=development

# Required for SDK
PROJECT_ID=your-project-id
NEAR_ACCOUNT=your-account.testnet
GITHUB_REPO=owner/repo
GITHUB_TOKEN=your-github-token

# Optional
LOG_LEVEL=info
TRACKING_INTERVAL=21600

# GitHub Configuration
GITHUB_TOKEN=your-github-token          # Required: Generate from GitHub Developer Settings
GITHUB_REPO=owner/repo                  # Required: Format: owner/repo
GITHUB_API_URL=https://api.github.com   # Optional: Default GitHub API URL

# NEAR Configuration
NEAR_ACCOUNT=your-account.near              # Required: Your NEAR account
NEAR_API_URL=https://api.nearblocks.io/v1   # Required: NEARBlocks API endpoint
NEAR_NETWORK_ID=mainnet                     # Required: 'mainnet' or 'testnet'
NEAR_NODE_URL=https://rpc.mainnet.near.org  # Optional: NEAR RPC node URL
NEAR_API_KEY=                               # Required: Get from https://docs.nearblocks.io/api

# SDK Configuration
PROJECT_ID=near-rewards                 # Required: Unique identifier for your project
LOG_LEVEL=info                          # Optional: debug, info, warn, error
METRICS_COLLECTION_INTERVAL=300000      # Optional: 5 minutes in milliseconds

# Storage Configuration
STORAGE_TYPE=postgres                   # Required: Currently only supports 'postgres'
STORAGE_RETENTION_DAYS=30               # Optional: Number of days to keep metrics

# Test Configuration
TEST_TIMEOUT=30000                      # Optional: Test timeout in milliseconds
ENABLE_API_MOCKING=false                # Optional: Use mocked API responses for testing
SKIP_VALIDATION=false                   # Optional: Skip metrics validation in tests
